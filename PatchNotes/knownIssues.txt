KNOWN ISSUES:
 - no sorting for Message CMS table
 - Ticket splitting doesn't work for wfComplete (does work for wfNew) 
 - Multiple messages for the same entry aren't overriding each other if the epArray isn't identical. (introduce intelligent parsing of episode detail arrays)
